(trigger
    (key [[alt][shift][cmd][down]]))

(name [Go to View])

(only-in [meta.rails.controller, meta.rails.mailer, source.js, source.css])

(input nothing)

(output tooltip)

(save nothing)

(script [RUBYLIB="$TM_BUNDLE_SUPPORT/lib:$RUBYLIB"
"${TM_RUBY:=ruby}" -- "${TM_BUNDLE_SUPPORT}/bin/go_to_alternate_file.rb" view])