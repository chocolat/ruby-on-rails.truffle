(trigger
    (key [[alt][esc]]))

(name [Autocomplete Foreign Key Fixture Reference])

(only-in [source.yaml, meta.rails.unit_test, meta.rails.functional_test])

(input selection line)

(output snippet)

(save nothing)

(script [RUBYLIB="$TM_BUNDLE_SUPPORT/lib:$RUBYLIB"
"${TM_RUBY:=ruby}" -- "${TM_BUNDLE_SUPPORT}/bin/fixture_auto_complete.rb" ])