(name [validates_uniqueness_of if])

(trigger
    (tab vuif))

(only-in source.ruby.rails)

(snippet [validates_uniqueness_of :%{1="attribute"}%{2=", :on => :%{3="create"}, :message => \"%{4="must be unique"}\", :if => proc { |obj| %{6="obj.condition?"} "}}])